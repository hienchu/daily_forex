name: Python Package using Conda

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    - name: update rates
      run: |
        POLYGON_API_KEY=h_0Cz1q_mUpYP5jOhCQeck3vWBQJUSP5 python main.py
        git add rates_*.json
        git commit -m "rates updated"
        git push
